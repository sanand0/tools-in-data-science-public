{
  "scripts": {
    "build": "./setup.sh",
    "check": "npm run check:links",
    "check:links": "node scripts/check-links.mjs",
    "lint": "dprint fmt -c https://raw.githubusercontent.com/sanand0/scripts/refs/heads/main/dprint.jsonc **/*.md"
  }
}
